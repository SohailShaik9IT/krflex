# Copy to .env for local `netlify dev` if needed.
# Database URL is auto-read from `.netlify/state.json` when using Netlify local DB.
# For a cloud Neon DB, set DATABASE_URL to your Neon connection string instead.
JWT_SECRET=replace-with-a-long-random-string
# DATABASE_URL=postgresql://user:password@host/db
